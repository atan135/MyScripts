# Script Index

| Script | Purpose | Entry |
| --- | --- | --- |
| `git-stats` | Summarize commit count and changed lines by day across all configured Git repositories. | `npm run git-stats` |
| `code-lines` | List code files above the configured line-count threshold in descending order. | `npm run code-lines` |
